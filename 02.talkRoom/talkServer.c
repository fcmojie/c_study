#include <stdio.h>
#include <stdlib.h>
#include <sys/socket.h>
#include <string.h>
#include <arpa/inet.h>
#include <sys/select.h>
#include <unistd.h>

int main(int argc, char const *argv[]) {
    // printf("argc:%d", argc);
    // printf("argv: %s", argv[1]);

    char *portstr = "";
    if (argc >= 2) {
        portstr = argv[1];
    }
    int port = 
    
    return 0;
}